package com.ahtesham.assignment.accountEventsAPI.entity;

public enum PeriodType {
	
	 TODAY, 
	 LAST_7_DAYS,
	 LAST_30_DAYS,
	 LAST_MONTH,
	 FEXIBLE_RANGE;
		
	
	/*
	 * TODAY "today", LAST_7_DAYS("Last 7 days"), LAST_30_DAYS("Last 30 days");
	 * 
	 * private String selection; private TransactionHistorySelectionType(String
	 * selection) { this.selection = selection; }
	 * 
	 * public String getSelection() { return selection; }
	 */
}
